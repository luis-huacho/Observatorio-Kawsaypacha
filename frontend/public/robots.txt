# Observatorio Kallpachakuy — https://predes.org.pe
#
# ⚠️ Esto es la RED, no el robots.txt que se sirve. El bueno lo genera el backend
# (backend/apps/sitio/descubrimiento.py) para que la línea `Sitemap:` apunte al dominio desde el
# que te lo estás descargando; nginx solo cae aquí si el backend no contesta.
#
# Y esa es justamente la razón de que la red exista: un 5xx en /robots.txt no hace que Google
# rastree el sitio entero, hace que DEJE de rastrearlo. Un backend reiniciándose no puede tener
# ese efecto, así que el peor caso es este archivo: permisivo, sin la línea `Sitemap:` —que no
# podemos escribir aquí sin clavarla a un dominio— y sin las señales de contenido.

User-agent: *
Allow: /
